{
    "configurations": [
    {
        "command": "npx vite --config ./tests/vite.config.ts  --host 0.0.0.0",
        "name": "Preview in Chrome",
        "request": "launch",
        "type": "node-terminal",
        "serverReadyAction": {
          "action": "debugWithChrome",
          "pattern": "Local:   http://localhost:([0-9]+)",
          "uriFormat": "http://localhost:%s",
          "webRoot": "${workspaceFolder}/apps/yacht-search/src"
        },
    }
    ]
}